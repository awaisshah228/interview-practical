/**
 * chapters/index.js — Language selector
 *
 * Change the import below to switch the tutorial language.
 *
 * Available:
 *   "./js/index.js"      — JavaScript (current)
 *
 * To add a new language:
 *   1. Create src/chapters/<lang>/data/*.json  (chapter JSON files)
 *   2. Create src/chapters/<lang>/index.js     (import + export chapters array)
 *   3. Change the import line below to "./<lang>/index.js"
 */
export { chapters } from "./js/index.js";
